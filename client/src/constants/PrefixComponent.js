/** @public
 *  @constructor
 *  @param {string} sComponentName
 *  @param {any} id */
export const PrefixId = (sComponentName, id) => ({

})[sComponentName];

/** @public
 *  @constructor
 *  @desc  Initialize and return id based on component name
 *  @param {string} sComponentName
 *  @param {any} id */
export const ConvertId = (sComponentName, id) => ({

})[sComponentName]